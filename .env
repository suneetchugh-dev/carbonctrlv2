# Copy this file to `.env` in the project root (same folder as package.json)
# Then fill in secrets and restart the dev server: pnpm dev

# Required for EcoBot (server-side only)
OPENAI_API_KEY=

# Optional demo value for /api/ping
PING_MESSAGE=ping
